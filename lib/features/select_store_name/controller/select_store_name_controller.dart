import 'package:dazaha_app/core/constant/exports_libraries.dart';

class SelectStoreNameController extends GetxController {
  // // late RxList<RxDouble> counters;
  // var countersIndex = 0.obs;

  // // void initializeCounters(int length) {
  // //   counters = List.generate(length, (_) => 1.0.obs).obs;
  // // }

  // void increment(int index) {
  //   countersIndex.value = index;
  //   countersIndex.value++;
  // }

  // void decrement(int index) {
  //   countersIndex.value = index;
  //   countersIndex.value--;
  //   // if (counters[index].value > 0) {
  //   //   counters[index].value--;
  //   // }
  // }
}
